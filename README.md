# 💱 API-Currency-exchange (FastAPI)

**API-Currency-exchange** —  микросервис для конвертации валют, построенный на FastAPI. Проект реализует полноценную систему аутентификации на основе JWT, интеграцию с внешним API для получения актуальных курсов валют и включает миграции базы данных через Alembic.

## 📋 Содержание

- [Основной функционал](#-основной-функционал)
- [Технологический стек](#-технологический-стек)
- [Структура проекта](#-структура-проекта)
- [Быстрый старт](#-быстрый-старт)
  - [Локальный запуск](#локальный-запуск)
  - [Запуск через Docker](#запуск-через-docker)
- [Переменные окружения](#-переменные-окружения)
- [Миграции базы данных](#-миграции-базы-данных)
- [API Эндпоинты](#-api-эндпоинты)
- [Тестирование](#-тестирование)
- [Обработка ошибок](#-обработка-ошибок)

## ✨ Основной функционал

### 🔐 Аутентификация и авторизация
- **Регистрация пользователей**: валидация уникальности имени
- **JWT аутентификация**: Bearer токены с настраиваемым временем жизни
- **Безопасное хранение**: пароли хешируются с использованием bcrypt
- **Защита эндпоинтов**: декораторы и зависимости FastAPI для проверки токенов

### 💰 Работа с валютами
- **Справочник валют**: полный список поддерживаемых валют
- **Актуальные курсы**: интеграция с внешним API курсов валют (https://marketplace.apilayer.com/currency_data-api)
- **Конвертация**:  расчет сумм с произвольными валютами

### ⚠️ Обработка исключений
- **Кастомные исключения**: специализированные классы ошибок для разных модулей
- **Централизованный хендлер**: единая система обработки ошибок
- 

## 🛠 Технологический стек

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **Backend** | FastAPI + Pydantic v2 | Веб-фреймворк и валидация |
| **Database** | PostgreSQL 15+ | Реляционная база данных |
| **ORM** | SQLAlchemy 2.0 | Взаимодействие с БД |
| **Migrations** | Alembic | Управление схемой БД |
| **Auth** | JWT + bcrypt | Аутентификация и безопасность |
| **Testing** | Pytest + pytest-mock | Модульное тестирование |
| **Container** | Docker + Docker Compose | Контейнеризация |
| **API** | REST | Архитектурный стиль |

## 📁 Структура проекта
API-Currency-exchange/  
│  
├── alembic/  
│ ├── versions/  
│ ├── env.py  
│ └── script.py.mako  
│  
├── app/  
│ ├── core/  
│ │ └── config.py  
│ │  
│ ├── database/  
│ │ ├── base.py  
│ │ └── database.py  
│ │  
│ ├── errors/  
│ │ ├── auth.py  
│ │ ├── base.py  
│ │ └── currency.py  
│ │  
│ ├── handlers/  
│ │ └── exceptions.py  
│ │  
│ ├── models/  
│ │ └── models.py  
│ │  
│ ├── routers/  
│ │ ├── auth.py  
│ │ └── currency.py  
│ │  
│ ├── schemas/  
│ │ ├── currency.py  
│ │ ├── errors.py  
│ │ └── users.py  
│ │  
│ ├── security/  
│ │ └── security.py  
│ │  
│ ├── utils/  
│ │ └── currency_data.py  
│ │  
│ └── main.py  
│  
├── tests/  
│ ├── **init**.py  
│ ├── conftest.py  
│ ├── test_auth.py  
│ └── test_currency.py  
│  
├── .dockerignore  
├── .env  
├── .gitignore  
├── alembic.ini  
├── compose.yml  
├── docker-entrypoint.sh  
├── Dockerfile  
├── pytest.ini  
├── README.md  
└── requirements.txt


## 🚀 Быстрый старт

### Предварительные требования
- Python 3.10+
- PostgreSQL 15+
- Docker и Docker Compose 

### Локальный запуск

1. **Клонирование репозитория**
   ```bash
   git clone https://github.com/Xzekexxx/API-Currency-exchange.git
   cd API-Currency-exchange
   ```

2. **Создание и активация виртуального окружения**
   ```bash
   # Windows
   python -m venv venv
   venv\Scripts\activate
   
   # macOS/Linux
   python3 -m venv venv
   source venv/bin/activate
   ```

3. **Установка зависимостей**
   ```bash
   pip install -r requirements.txt
   ```

4. **Запуск приложения**
   ```bash
   uvicorn app.main:app --reload
   ```

### Запуск через Docker

```bash
# 1. Клонирование и переход в директорию
git clone https://github.com/Xzekexxx/API-Currency-exchange.git
cd API-Currency-exchange

# 2. Запуск контейнеров
docker-compose up

# 3. Остановка контейнеров
docker-compose down
```

**Приложение доступно:** [http://localhost:8000](http://localhost:8000)  
**Документация Swagger:** [http://localhost:8000/docs](http://localhost:8000/docs)
**Графический интерфейс pgAdmin 4:** [http://localhost:5050](http://localhost:5050)

## Переменные окружения

Для корректной работы создайте файл **.env** в корневом каталоге и заполните его:

```env
SECRET_KEY=SECRET_KEY(генерируется командой openssl rand -hex 32)

ALGORITHM=HS256

ACCESS_TOKEN_EXPIRE_MINUTES=30

DB_HOST=127.0.0.1

DB_PORT=5432

DB_USER=postgres

DB_PASS=mypassword

DB_NAME=my_db

API_KEY=your_api_key
```

## Миграции базы данных

Проект использует Alembic для управления миграциями БД.

**Создание новой миграции:**
```bash
alembic revision --autogenerate -m "описание изменений"
```

**Применение миграций:**
```bash
alembic upgrade head
```

## Тестирование

Для запуска тестов в корневой папке выполните команду:
```bash
pytest
```
## 📋 API Эндпоинты
| Метод | Endpoint | Описание |
|-------|---------|----------|
| `POST` | `/reg` | Регистрация пользователя |
| `POST` | `/login` | Вход пользователя в систему |
| `GET` | `/user/about_user` | Данные пользователя (имя, почта) |
| `GET` | `/currency/list` | Список доступных валют |
| `GET` | `/currency/actual_rates` | Актуальные курсы валют по отношению к доллару |
| `GET` | `/currency/converter` | Конвертация валют (из одной валюты в другую с указанной суммой) |